.cell {
  --h: 120rpx;
  --background: #FFF;
  height: var(--h);
  background: var(--background);
  display: flex;
  align-items: center;
  box-sizing: border-box;
  padding: 0 30rpx;
}
.cell-content {
  flex: 1;
}
